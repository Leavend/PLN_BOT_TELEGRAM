# URL server PLN region balikpapan — domain permanen (named/dashboard tunnel).
# Server balikpapan sudah hidup lagi (cloudflared service install + supervisor),
# tidak lagi di-reroute ke bontang. Petugas cukup `fasih-update` (git pull).
# Baris pertama non-komentar = URL aktif.
https://balikpapan.xyzfasih.site
